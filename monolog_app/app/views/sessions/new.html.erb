<% provide(:title, "Log in") %>
<div class="container-fluid head">
  <div class="col-xs-12 col-sm-12  col-md-6 col-md-offset-4">
    <h1>Monologにログインする</h1>
  </div>

  <div class="row">
    <div class="col-xs-11 col-xs-offset-1 col-sm-7  col-md-4 col-md-offset-4">
      <p>こちらのサイトは時間、人を気にせずに情報の発散ができます。</p>
    </div>
  </div>
</div>

<div class="container-fluid main">
  <div class="row">
    <div class="col-sm-7 col-sm-offset-3 col-md-5 col-md-offset-3">
      <div class="container-fluid kuhaku2"></div>

      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>

        <%= form_for(:session, url: login_path) do |f| %>
          <%= f.label :user_id, "ユーザーID" %>
          <%= f.text_field :user_id, class: 'form-control', placeholder: "ユーザーID" %>
          <div class="container-fluid kuhaku2"></div>     
          <%= f.label :password %>
          <%= f.password_field :password, class: 'form-control', placeholder: "パスワード"  %>
          <div class="container-fluid kuhaku2"></div>
          <%= f.label :remember_me, class: "checkbox inline" do %>
          <%= f.check_box :remember_me %>
          <span>ログイン状態を維持する</span>
          <% end %>
          <%= f.submit "ログイン", class: "btn btn-default"%>
        <% end %>
      <p>アカウントを持ってない? <%= link_to "今すぐ登録！", signup_path %></p>
    </div>
  </div>
</div>
<div class="footer"></div>


