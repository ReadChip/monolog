<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="flex-container">
    <%= f.text_area :content, placeholder: "エミットする？", :size =>"80x3" %>
    <%= f.submit "Emit", class: "btn btn-warning" %>
  </div>

<% end %>