<!--ログインしているときのTop画面-->
<% if logged_in? %>
  <!--空白-->
  <div class="container-fluid kuhaku"></div>

  <%= render 'shared/flash' %>

  <!--左のメニューです（ここに自分の名前やらサムネなど入る予定です）-->
  <div class="container">
      <div class="row">
        <div class="col-sm-4  col-md-3  col-lg-3">
          <section class="user_info">
            <%= render 'shared/user_info' %>
          </section>      
             
        </div>

        <div style="display:none;">
          <%= form_for(@user, url: refresh_path, method: 'get', remote: true) do |f| %>
            <% f.submit(id: "refresh") %>
          <% end %>
        </div>
        <script>
          clearInterval(id);
          var id = setInterval(function(){document.getElementById("refresh").click();}, 5000);
        </script>

        <!--%= link_to "更新", refresh_path, remote: true %-->

        <div class="col-sm-8  col-md-8  col-lg-7">
          <section class="micropost_form">
            <%= render 'shared/micropost_form' %>
          </section>

          <!--%=raw "<meta http-equiv='refresh' content='10000';>" %-->  

          <div id="feed">
          <%= render 'shared/feed' %>
          </div>

        </div>
      </div>
  </div>

</div>
<% else %>
  <!--TOP画面（ログイン前）-->

  <div class="head">
    <div class="row">
      <div class="col-xs-10 col-xs-offset-1   col-sm-10 col-sm-offset-2   col-md-9 col-md-offset-3    col-lg-9 col-lg-offset-3">
        <h1>Monolog</h1>
      </div>
    </div>

    <div class="row">
      <div class="col-xs-10 col-xs-offset-1  col-sm-4 col-sm-offset-2    col-md-3 col-md-offset-3    col-lg-3 col-lg-offset-3">
        <p>ゆるく繋がるSNS。暇つぶしに:D</p>
      </div>
      <div class="col-xs-10 col-xs-offset-1    col-sm-5 col-sm-offset-1   col-md-5 col-md-offset-1     col-lg-5 col-lg-offset-1">
        <button type="button" data-toggle="modal" class="btn btn-warning"  data-target="#create">アカウント作成</button>
          <div class="modal fade" id="create" tabindex="-1" role="dialog" aria-labelledby="create" aria-hidden="true">
            <div class="modal-dialog" role="document">
              <div class="modal-content">
                <div class="modal-header">
                  <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">&times;</span>
                  </button>
                  <h4 class="modal-title" id="modal-label">Monologに登録</h4>
                </div>
                <div class="modal-body">
                  <%= render 'shared/top_head_new' %>
                </div>
              </div>
            </div>
          </div>
          <button type="button" data-toggle="modal" data-target="#exampleModal" class="btn btn-warning">ログイン</button>
          <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
              <div class="modal-content">
                <div class="modal-header">
                  <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">&times;</span>
                  </button>
                  <h4 class="modal-title" id="modal-label">Monologにログイン</h4>
                </div>
                <div class="modal-body">
                  <%= render 'shared/top_head_login' %>
                </div>
              </div>
            </div>
            </div>
          </div>
        </div>
      </div>
  </div>
  <%= render 'shared/flash' %>

  <div class="main">
    <div class="row">
      <div class="col-xs-10 col-xs-offset-1    col-sm-3 col-sm-offset-1    col-md-3 col-md-offset-3    col-lg-3 col-lg-offset-3">
        <%= image_tag 'top.png', :width => '300', :height => '200' %>  
      </div>
      <div class="col-xs-10 col-xs-offset-1   col-sm-6 col-sm-offset-1   col-md-4 col-md-offset-1     col-lg-2 col-lg-offset-1">
        <p>アカウントを作成する</p>
        <%= render 'shared/top_body_new' %>
      </div>
    </div>
  </div>

  <div class="footer">
    <div class="row">
      <div class="col-sm-10 col-sm-offset-2   col-md-9 col-md-offset-3   col-lg-9 col-lg-offset-3">
        <h3>Monologの特徴</h3>
      </div>
    </div>
    <div class="row">
      <div class="col-sm-4 col-sm-offset-2   col-md-4 col-md-offset-3    col-lg-3 col-lg-offset-3">
        <div class="slick-box">
          <div><%= image_tag '1.png', width: '100%', height: '300px'%></div>
          <div><%= image_tag '2.png', width: '100%', height: '300px'%></div>
          <div><%= image_tag '3.png', width: '100%', height: '300px'%></div>
        </div>
      </div>
      <div class="col-sm-4 col-sm-offset-1   col-md-3  col-md-offset-1   col-lg-2  col-lg-offset-1">
        <p>1. Monologにはフォロー、フォロワーの概念がなくMonologに登録されている全ユーザの投稿の中からランダムにタイムラインに表示されます。</p>
        <p>2. 返信機能が無いので、他人の意見を気にせずあなたが言いたいことを投稿してもMonologで炎上することはありません。</p>
        <p>3. あなたが投稿した内容は0~6時間遅らせて投稿されるので時間、場所を気にせず投稿できます。6時間を過ぎるとタイムラインに流れることはありません。</p>
      </div>    
    </div>
  </div>

<% end %>